#include <stdio.h>
int main(){

	float a,b;
	printf("erste Zahl eingeben:");
	scanf("%f",&a);
	printf("erste Zahl eingeben:");
	scanf("%f",&b);
	printf("die Summe aus %f und %f ist %.3f",a,b,a+b);
	return 0;
}
